create envoriment
conda create --name mydashenv python=3.06
is any path issue (the set path )
activate envoriment
activate mydashenv
install required package(file given)
pip install -r requirements.txt

